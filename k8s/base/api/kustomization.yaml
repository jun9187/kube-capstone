apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- deployment.yaml
- service.yaml

configMapGenerator:
- name: api-config
  literals:
  - JWT_EXPIRE_MINUTES=60
  - CACHE_TTL_SECONDS=15

secretGenerator:
- name: api-secrets
  envs:
  - secrets.env